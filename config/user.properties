# =====================================================================
# Web Performance Framework - JMeter engine user.properties
#   jmeter -p config/user.properties -n -t jmx/LoadTest.jmx ...
# On BlazeMeter/RedLine13 these go in the JMeter Properties box (or omit
# and let the platform manage result saving).
# =====================================================================

# ---- Result log format ----------------------------------------------
jmeter.save.saveservice.output_format=csv
jmeter.save.saveservice.response_data=false
jmeter.save.saveservice.response_data.on_error=true
jmeter.save.saveservice.samplerData=false
jmeter.save.saveservice.requestHeaders=false
jmeter.save.saveservice.responseHeaders=false
jmeter.save.saveservice.assertions=true
jmeter.save.saveservice.latency=true
jmeter.save.saveservice.connect_time=true
jmeter.save.saveservice.thread_counts=true
jmeter.save.saveservice.sub_results=true
jmeter.save.saveservice.bytes=true
jmeter.save.saveservice.url=true
jmeter.save.saveservice.label=true
jmeter.save.saveservice.code=true
jmeter.save.saveservice.message=true
jmeter.save.saveservice.successful=true
jmeter.save.saveservice.timestamp_format=ms
jmeter.save.saveservice.print_field_names=true

# ---- HTML dashboard percentiles / apdex -----------------------------
jmeter.reportgenerator.apdex_satisfied_threshold=2000
jmeter.reportgenerator.apdex_tolerated_threshold=4000
jmeter.reportgenerator.overall_granularity=5000
jmeter.reportgenerator.report_title=Web Performance Report
aggregate_rt_pct1=90
aggregate_rt_pct2=95
aggregate_rt_pct3=99

# ---- Browser-like engine behaviour ----------------------------------
# Keep embedded resource sub-results so per-asset timing is visible.
httpclient4.retrycount=0
http.connection.stalecheck$Boolean=true
# Do NOT fail the whole page if one embedded image 404s
httpsampler.ignore_failed_embedded_resources=true
# Concurrent pool for parallel asset downloads (browser uses ~6)
jmeterengine.force.system.exit=true

# ---- Cookie / cache realism -----------------------------------------
CookieManager.save.cookies=true
CookieManager.check.cookies=false

# ---- Console heartbeat in non-GUI runs ------------------------------
summariser.name=summary
summariser.interval=30
summariser.log=true
summariser.out=true
